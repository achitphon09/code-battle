return (
    <div className="relative h-screen w-full">
      <img
        src="/coding.jpg"
        className="absolute inset-0 h-full w-full object-cover z-0"
        alt="Background"
      />

      <div className="relative z-10 flex h-full items-center justify-center px-4 sm:px-6 lg:px-8">
        <div className="bg-white/50 backdrop-blur-xs rounded-xl shadow-lg p-6 sm:p-8 w-full max-w-md mx-auto">
          <img src="/CB_logo5.nobg.png" className="w-24 mx-auto mb-4" />
          <p className="text-black text-xl font-medium mb-6 text-center">
            Login
          </p>
          <form onSubmit={handleSubmit} className="space-y-4">
            {error && (
              <div className="bg-red-500 text-white p-2 rounded-md text-sm sm:text-base">{error}</div>
            )}

            <div>
              <label className="block mb-1 text-sm font-medium text-gray-900">Username or Email</label>
              <input
                onChange={(e) => setIdentifier(e.target.value)}
                type="text"
                placeholder="Username or Email"
                className="w-full p-3 rounded-lg font-light text-white
                  border
                  focus:outline-none focus:bg-black/50 focus:border-white/50 focus:ring-2 focus:ring-white 
                  focus:shadow-md focus:shadow-white/30  
                  transition-all duration-300 ease-in-out"
                required
              />
            </div>

            <div>
              <label className="block mb-1 text-sm font-medium text-gray-900">Password</label>
              <input
                onChange={(e) => setPassword(e.target.value)}
                type="password"
                placeholder="Password"
                className="w-full p-3 rounded-lg font-light text-white 
                  border
                  focus:outline-none focus:bg-black/50 focus:border-white/50 focus:ring-2 focus:ring-white 
                  focus:shadow-md focus:shadow-white/30  
                  transition-all duration-300 ease-in-out"
                required
              />
            </div>

            <button
              type="submit"
              className="w-full bg-gradient-to-r from-red-600 via-purple-600 to-blue-600 text-white p-3 rounded-lg hover:opacity-80 transition duration-200 cursor-pointer"
            >
              Login
            </button>

            <p className="text-black text-xs sm:text-sm text-center font-light mt-4">Don't have an account ?
                <a href="/register" className="text-blue-700 font-light m-2 hover:underline ml-1">Register</a>
            </p>
          </form>
        </div>
      </div>
      </div>
  )
}
